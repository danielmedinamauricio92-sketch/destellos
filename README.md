# destellos
A digital space for fragments, thoughts and moments.

## Deploy en Vercel

La app Next.js vive dentro de la carpeta `destellos/`.

Al importar este repositorio en Vercel, configurar:

- Framework Preset: `Next.js`
- Root Directory: `destellos`
- Install Command: `npm install`
- Build Command: `npm run build`
